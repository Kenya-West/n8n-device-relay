# config-model

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build config-model` to build the library.

## Running unit tests

Run `nx test config-model` to execute the unit tests via [Vitest](https://vitest.dev/).
